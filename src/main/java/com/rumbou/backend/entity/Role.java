package com.rumbou.backend.entity;

// USER: postulante. REVIEWER: revisa y aprueba preguntas generadas por IA, sin
// administrar el sistema. ADMIN: todo, incluido el CRUD de preguntas y los roles.
public enum Role {
    USER,
    REVIEWER,
    ADMIN
}
